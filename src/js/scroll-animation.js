// document.addEventListener("DOMContentLoaded", function () {
//   const wrapper = document.querySelector(".prototype-animation-wr");
//   const items = document.querySelectorAll(".proto-animation");

//   // Duplicar elementos
//   items.forEach((item) => {
//     const clone = item.cloneNode(true);
//     wrapper.appendChild(clone);
//   });

//   // Configurar animação
//   wrapper.style.animation = "scroll 5s linear infinite";
// });
